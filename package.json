{
  "name": "riksutin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "workspaces": [
    "src/client",
    "src/server",
    "src/validators",
    "tests"
  ],
  "scripts": {
    "start": "docker compose up",
    "start:prod": "NODE_ENV=production npm run build --workspace=server",
    "start:test": "NODE_ENV=test npm run build --workspace=server",
    "start:local:test": "ORGANISATION_ID=hy REACT_APP_CONTROL_REPORT_CHECK_ENABLED=true npm run build && docker compose -f docker-compose.ci.yml -f docker-compose.test.yml up",
    "start:dev": "NODE_ENV=development concurrently \"npm run start:dev:server\" \"npm run start:dev:client\"",
    "start:dev:server": "npm run watch --workspace=server",
    "start:dev:client": "npm run watch --workspace=client",
    "docker:down": "docker compose -f docker-compose.ci.yml -f docker-compose.test.yml down",
    "test": "npm run test --workspaces --if-present",
    "test:local": "npx playwright test",
    "test:api": "npx playwright test tests/api.spec.ts",
    "lint": "oxlint",
    "format": "prettier --write \"*.{ts,tsx,json,css,md}\"",
    "build": "npm run build --workspace=client",
    "prepare": "husky install",
    "build:local:test": "docker exec -it riksutin_test npm run build",
    "check:migrations": "node scripts/analyzeMigrationNames.js",
    "check:translations": "node scripts/analyzeTranslations.js"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/jest": "^27.5.2",
    "@types/prettier": "^2.7.2",
    "concurrently": "^7.6.0",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-prettier": "^5.2.3",
    "globals": "^17.7.0",
    "husky": "^8.0.3",
    "lint-staged": "^13.3.0",
    "minimist": "1.2.8",
    "oxlint": "1.75.0",
    "oxlint-tsgolint": "7.0.2001",
    "prettier": "^3.5.1"
  },
  "proxy": "http://localhost:8000",
  "lint-staged": {
    "*.{js,ts,tsx}": "npm run lint"
  }
}
